[package]
name = "jslt-node"
version = "0.1.0"
edition = "2021"
exclude = ["bin/*.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
jslt = { path = "..", version = "0.1" }

neon = { version = "1.0.0-alpha.4", default-features = false, features = ["napi-8"] }
serde_json = "1"
