[package]
name = "jslt-node"
version = "0.1.0"
edition = "2021"
exclude = ["index.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
jslt = { path = ".." }

neon = { version = "0.10", default-features = false, features = ["napi-6"] }
serde_json = "1"
